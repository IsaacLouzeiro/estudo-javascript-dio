// 01) Crie um programa que dado um numero imprima a sua tabuada.

const numero = 2;
let soma;
for(let i = 1; i <= 10; i++) {

    soma = numero * i;

    console.log(numero + ' X ' + i + ' = ' + soma);

}